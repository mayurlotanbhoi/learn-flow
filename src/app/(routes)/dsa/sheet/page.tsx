import { NextPage } from 'next'

interface Props { }

const Page: NextPage<Props> = ({ }) => {
     return <div className='w-screen bg-amber-50 text-white mt-40'>DSA Sheet</div>
}

export default Page